# Makefile for BiTun ESP32 Component

.PHONY: all build clean

all: build

build:
	@echo "To compile this component into a static library (.a), integrate this folder"
	@echo "into your ESP-IDF project components/ directory and run: idf.py build"

clean:
	rm -f *.o *.a
